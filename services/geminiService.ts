// Service removed as AI features are disabled.
// This file is kept as a placeholder to prevent import errors if referenced elsewhere, 
// though it should not be used.

export const getPlanRecommendation = async (userDescription: string) => {
  return null;
};